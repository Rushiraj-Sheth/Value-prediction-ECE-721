91c91
< === VALUE PREDICTOR =============================================================
---
> === VALUE PREDICTOR ============================================================
97c97,98
< VALUE PREDICTOR = stride (Project 4)
---
> 
> VALUE PREDICTOR = stride (Project 4 spec. implementation)
106,111c107,112
< 		tag              :   0 bits
< 		conf             :   2 bits
< 		retired_value    :  64 bits
< 		stride           :  64 bits
< 		instance ctr     :   8 bits
< 		---------------------------
---
>       tag           :   0 bits  // num_tag_bits
>       conf          :   2 bits  // formula: (uint64_t)ceil(log2((double)(confmax+1)))
>       retired_value :  64 bits  // RISCV64 integer size.
>       stride        :  64 bits  // RISCV64 integer size. Competition opportunity: truncate stride to far fewer bits based on stride distribution of stride-predictable instructions.
>       instance ctr  :   8 bits  // formula: (uint64_t)ceil(log2((double)VPQsize))
>       -------------------------
113c114,116
< 	Total storage cost (bits)  (16 SVP entries x 138 bits/SVP entry) = 2208
---
>    Total storage cost (bits) = (16 SVP entries x 138 bits/SVP entry) = 2208 bits
>    Total storage cost (bytes) = 276.00 B (0.27 KB)
> 
137a141
> VPQ notes: (1) I don't have a valid bit to go with the computed value, so I can't print it in a meaningful way. Values are produced OOO; some are available, others are not, and I can't tell which. (2) I include the full PC for reference, even though only PCtag and PCindex are needed. (3) I print entries between [head,tail). Thus, no entries are displayed if the VPQ is empty.
236a241
> VPQ notes: (1) I don't have a valid bit to go with the computed value, so I can't print it in a meaningful way. Values are produced OOO; some are available, others are not, and I can't tell which. (2) I include the full PC for reference, even though only PCtag and PCindex are needed. (3) I print entries between [head,tail). Thus, no entries are displayed if the VPQ is empty.
335a341
> VPQ notes: (1) I don't have a valid bit to go with the computed value, so I can't print it in a meaningful way. Values are produced OOO; some are available, others are not, and I can't tell which. (2) I include the full PC for reference, even though only PCtag and PCindex are needed. (3) I print entries between [head,tail). Thus, no entries are displayed if the VPQ is empty.
434a441
> VPQ notes: (1) I don't have a valid bit to go with the computed value, so I can't print it in a meaningful way. Values are produced OOO; some are available, others are not, and I can't tell which. (2) I include the full PC for reference, even though only PCtag and PCindex are needed. (3) I print entries between [head,tail). Thus, no entries are displayed if the VPQ is empty.
533a541
> VPQ notes: (1) I don't have a valid bit to go with the computed value, so I can't print it in a meaningful way. Values are produced OOO; some are available, others are not, and I can't tell which. (2) I include the full PC for reference, even though only PCtag and PCindex are needed. (3) I print entries between [head,tail). Thus, no entries are displayed if the VPQ is empty.
632a641
> VPQ notes: (1) I don't have a valid bit to go with the computed value, so I can't print it in a meaningful way. Values are produced OOO; some are available, others are not, and I can't tell which. (2) I include the full PC for reference, even though only PCtag and PCindex are needed. (3) I print entries between [head,tail). Thus, no entries are displayed if the VPQ is empty.
731a741
> VPQ notes: (1) I don't have a valid bit to go with the computed value, so I can't print it in a meaningful way. Values are produced OOO; some are available, others are not, and I can't tell which. (2) I include the full PC for reference, even though only PCtag and PCindex are needed. (3) I print entries between [head,tail). Thus, no entries are displayed if the VPQ is empty.
830a841
> VPQ notes: (1) I don't have a valid bit to go with the computed value, so I can't print it in a meaningful way. Values are produced OOO; some are available, others are not, and I can't tell which. (2) I include the full PC for reference, even though only PCtag and PCindex are needed. (3) I print entries between [head,tail). Thus, no entries are displayed if the VPQ is empty.
929a941
> VPQ notes: (1) I don't have a valid bit to go with the computed value, so I can't print it in a meaningful way. Values are produced OOO; some are available, others are not, and I can't tell which. (2) I include the full PC for reference, even though only PCtag and PCindex are needed. (3) I print entries between [head,tail). Thus, no entries are displayed if the VPQ is empty.
1028a1041
> VPQ notes: (1) I don't have a valid bit to go with the computed value, so I can't print it in a meaningful way. Values are produced OOO; some are available, others are not, and I can't tell which. (2) I include the full PC for reference, even though only PCtag and PCindex are needed. (3) I print entries between [head,tail). Thus, no entries are displayed if the VPQ is empty.
1127a1141
> VPQ notes: (1) I don't have a valid bit to go with the computed value, so I can't print it in a meaningful way. Values are produced OOO; some are available, others are not, and I can't tell which. (2) I include the full PC for reference, even though only PCtag and PCindex are needed. (3) I print entries between [head,tail). Thus, no entries are displayed if the VPQ is empty.
1226a1241
> VPQ notes: (1) I don't have a valid bit to go with the computed value, so I can't print it in a meaningful way. Values are produced OOO; some are available, others are not, and I can't tell which. (2) I include the full PC for reference, even though only PCtag and PCindex are needed. (3) I print entries between [head,tail). Thus, no entries are displayed if the VPQ is empty.
1325a1341
> VPQ notes: (1) I don't have a valid bit to go with the computed value, so I can't print it in a meaningful way. Values are produced OOO; some are available, others are not, and I can't tell which. (2) I include the full PC for reference, even though only PCtag and PCindex are needed. (3) I print entries between [head,tail). Thus, no entries are displayed if the VPQ is empty.
1424a1441
> VPQ notes: (1) I don't have a valid bit to go with the computed value, so I can't print it in a meaningful way. Values are produced OOO; some are available, others are not, and I can't tell which. (2) I include the full PC for reference, even though only PCtag and PCindex are needed. (3) I print entries between [head,tail). Thus, no entries are displayed if the VPQ is empty.
1523a1541
> VPQ notes: (1) I don't have a valid bit to go with the computed value, so I can't print it in a meaningful way. Values are produced OOO; some are available, others are not, and I can't tell which. (2) I include the full PC for reference, even though only PCtag and PCindex are needed. (3) I print entries between [head,tail). Thus, no entries are displayed if the VPQ is empty.
1622a1641
> VPQ notes: (1) I don't have a valid bit to go with the computed value, so I can't print it in a meaningful way. Values are produced OOO; some are available, others are not, and I can't tell which. (2) I include the full PC for reference, even though only PCtag and PCindex are needed. (3) I print entries between [head,tail). Thus, no entries are displayed if the VPQ is empty.
1721a1741
> VPQ notes: (1) I don't have a valid bit to go with the computed value, so I can't print it in a meaningful way. Values are produced OOO; some are available, others are not, and I can't tell which. (2) I include the full PC for reference, even though only PCtag and PCindex are needed. (3) I print entries between [head,tail). Thus, no entries are displayed if the VPQ is empty.
1820a1841
> VPQ notes: (1) I don't have a valid bit to go with the computed value, so I can't print it in a meaningful way. Values are produced OOO; some are available, others are not, and I can't tell which. (2) I include the full PC for reference, even though only PCtag and PCindex are needed. (3) I print entries between [head,tail). Thus, no entries are displayed if the VPQ is empty.
1919a1941
> VPQ notes: (1) I don't have a valid bit to go with the computed value, so I can't print it in a meaningful way. Values are produced OOO; some are available, others are not, and I can't tell which. (2) I include the full PC for reference, even though only PCtag and PCindex are needed. (3) I print entries between [head,tail). Thus, no entries are displayed if the VPQ is empty.
2018a2041
> VPQ notes: (1) I don't have a valid bit to go with the computed value, so I can't print it in a meaningful way. Values are produced OOO; some are available, others are not, and I can't tell which. (2) I include the full PC for reference, even though only PCtag and PCindex are needed. (3) I print entries between [head,tail). Thus, no entries are displayed if the VPQ is empty.
2138,2144c2161,2167
< vpmeas_ineligible         : 10000
< vpmeas_eligible           : 10000
< 	vpmeas_miss            : 0
< 	vpmeas_conf_corr       : 9966
< 	vpmeas_conf_incorr     : 0
< 	vpmeas_unconf_corr     : 0
< 	vpmeas_unconf_incorr   : 34
---
> vpmeas_ineligible         :      10000 ( 50.00%) // Not eligible for value prediction.
> vpmeas_eligible           :      10000 ( 50.00%) // Eligible for value prediction.
>    vpmeas_miss            :          0 (  0.00%) // VPU was unable to generate a value prediction (e.g., SVP miss).
>    vpmeas_conf_corr       :       9966 ( 49.83%) // VPU generated a confident and correct value prediction.
>    vpmeas_conf_incorr     :          0 (  0.00%) // VPU generated a confident and incorrect value prediction. (MISPREDICTION)
>    vpmeas_unconf_corr     :          0 (  0.00%) // VPU generated an unconfident and correct value prediction. (LOST OPPORTUNITY)
>    vpmeas_unconf_incorr   :         34 (  0.17%) // VPU generated an unconfident and incorrect value prediction.
\ No newline at end of file
